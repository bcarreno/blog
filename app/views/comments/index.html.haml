%h1 Listing comments

%table
  %tr
    %th Name
    %th Email
    %th Body
    %th Article
    %th
    %th
    %th

  - @comments.each do |comment|
    %tr
      %td= comment.name
      %td= comment.email
      %td= comment.body
      %td= comment.article_id
      %td= link_to 'Show', comment
      %td= link_to 'Edit', edit_comment_path(comment)
      %td= link_to 'Delete', comment, :confirm => 'Are you sure?', :method => :delete

%p= link_to 'New Comment', new_comment_path
