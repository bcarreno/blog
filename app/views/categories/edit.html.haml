- set_title 'Editing category'

= render 'form'

%p
  = link_to 'Show', @category
  \|
  = link_to 'Delete', @category, data: { :confirm => 'Are you sure?' }, :method => :delete
  \|
  = link_to 'New Category', new_category_path
  \|
  = link_to 'List', categories_path
