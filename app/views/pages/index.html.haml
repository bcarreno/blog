%h1 Listing pages

%table
  %tr
    %th Title
    %th Markup
    %th
    %th
    %th

  - @pages.each do |page|
    %tr
      %td= page.title
      %td= page.markup
      %td= link_to 'Show', page
      %td= link_to 'Edit', edit_page_path(page)
      %td= link_to 'Delete', page, :confirm => 'Are you sure?', :method => :delete

%p= link_to 'New Page', new_page_path
